web:
  build: .
  container_name: api-nfl-schedule
  ports:
    - 4101:4101
  environment:
    - PORT=4101
    - REDIS_HOST=redis
    - REDIS_PORT=6379
  volumes:
    - ./app:/src/app
  links:
    - redis
redis:
  image: redis
  container_name: redis
  ports:
    - 6379:6379